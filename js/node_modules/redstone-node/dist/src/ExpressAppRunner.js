"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.ExpressAppRunner = void 0;
var express_1 = __importDefault(require("express"));
var logger = require("./utils/logger")("express");
var PORT = 8080;
// For now we run a very simple express app to allow running the
// redstone node in the AWS App runner.
// Read more at:
// This class will be extended in future and will be used for
// communication between nodes
var ExpressAppRunner = /** @class */ (function () {
    function ExpressAppRunner() {
        this.app = (0, express_1.default)();
        this.app.get("/", function (_req, res) {
            res.send("Hello App Runner. My name is RedStone node and I am doing good ;)");
        });
    }
    ExpressAppRunner.prototype.run = function () {
        if (process.env.NODE_ENV === "test") {
            logger.info("Express server running skipped in test environment");
            return;
        }
        logger.info("Running express server on port: ".concat(PORT));
        this.app.listen(PORT, function () {
            logger.info("Server started at http://localhost:".concat(PORT));
        });
    };
    return ExpressAppRunner;
}());
exports.ExpressAppRunner = ExpressAppRunner;
