import { Broadcaster } from "../Broadcaster";
import { PriceDataSigned, SignedPricePackage } from "../../types";
export declare class StreamrBroadcaster implements Broadcaster {
    private streamrProxy;
    private pricesToBroadcast;
    private packageToBroadcast;
    private timer?;
    constructor(ethereumPrivateKey: string);
    private lazyEnableTimer;
    private broadcastInternal;
    broadcast(prices: PriceDataSigned[]): Promise<void>;
    broadcastPricePackage(signedData: SignedPricePackage, _providerAddress: string): Promise<void>;
}
