export declare class StreamrProxy {
    private streamrClient;
    constructor(ethPrivateKey: string);
    tryCreateStream(name: string): Promise<string>;
    publishToStreamByName(data: any, streamName: string): Promise<void>;
    private publish;
    private getStreamIdForStreamName;
    private createStream;
    private doesStreamExist;
}
