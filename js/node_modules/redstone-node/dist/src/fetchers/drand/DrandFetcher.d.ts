import { PricesObj } from "../../types";
import { BaseFetcher } from "../BaseFetcher";
export declare const ENTROPY_SYMBOL = "ENTROPY";
export declare class DrandFetcher extends BaseFetcher {
    constructor();
    fetchData(): Promise<import("axios").AxiosResponse<any>>;
    extractPrices(response: any, symbols: string[]): Promise<PricesObj>;
}
