"use strict";
var __spreadArray = (this && this.__spreadArray) || function (to, from, pack) {
    if (pack || arguments.length === 2) for (var i = 0, l = from.length, ar; i < l; i++) {
        if (ar || !(i in from)) {
            if (!ar) ar = Array.prototype.slice.call(from, 0, i);
            ar[i] = from[i];
        }
    }
    return to.concat(ar || Array.prototype.slice.call(from));
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.timeout = exports.readJSON = exports.mergeObjects = void 0;
var fs_1 = __importDefault(require("fs"));
function mergeObjects(objects) {
    return Object.assign.apply(Object, __spreadArray([{}], objects, false));
}
exports.mergeObjects = mergeObjects;
function readJSON(path) {
    var content = fs_1.default.readFileSync(path, "utf-8");
    try {
        return JSON.parse(content);
    }
    catch (e) {
        throw new Error("File \"".concat(path, "\" does not contain a valid JSON"));
    }
}
exports.readJSON = readJSON;
function timeout(ms) {
    return new Promise(function (resolve) { return setTimeout(function () { return resolve('timeout'); }, ms); });
}
exports.timeout = timeout;
